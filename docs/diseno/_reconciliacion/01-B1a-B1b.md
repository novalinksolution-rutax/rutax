# Reconciliación 01 · B1a + B1b

Tableros: `Rutax B1a Monitoreo.dc.html` (Torre de control · Preparación del día) y
`Rutax B1b Incidencias y manifiestos.dc.html` (Incidencias · Manifiestos · Detalle del
manifiesto).

Método: `COMO-SE-HACE.md`. **Manda el tablero.** Donde el tablero contradice una regla
vigente del proyecto se anota la contradicción y no se resuelve acá.

## Resumen

| # | Pantalla | Ruta real | Veredicto |
|---|---|---|---|
| 1 | Torre de control | `src/app/(tenant)/torre-de-control/` | `FALTA PIEZA` |
| 2 | Preparación del día | `src/app/(tenant)/preparacion/` | `FALTA PIEZA` |
| 3 | Incidencias | `src/app/(tenant)/operaciones/incidencias/` | `PANTALLA DISTINTA` |
| 4 | Manifiestos (listado) | `src/app/(tenant)/manifiestos/` | `FALTA PIEZA` |
| 5 | Detalle del manifiesto (secuenciador) | `src/app/(tenant)/manifiestos/[manifiestoId]/` | `FALTA PIEZA` |

---

## 1 · Torre de control

**Ruta real:** `src/app/(tenant)/torre-de-control/` — `page.tsx` (guard RBAC + composer),
`_componentes/torre.tsx` (orquestador), `cifras.tsx`, `panel.tsx`, `mapa.tsx`, `ficha.tsx`,
`placas.tsx`, `lista-comunas.tsx`, `banda-olas.tsx`, `marcadores-conductor.tsx`.

### Qué muestra el tablero

Patrón «panel de monitoreo en vivo», 1440 oscuro. De arriba abajo: encabezado con título,
línea de contexto (`21-08 16:04 · cuenta lo que declaró el conductor`) y tres distintivos a la
derecha — **SOLO LECTURA** (con trama), **en vivo**, **Pantalla completa**. Debajo, una franja
de **cuatro magnitudes**: `POR ENTREGAR HOY 38 de 120` · `EN RUTA AHORA 34` ·
`CONDUCTORES CON RUTA 7 de 9` · `INCIDENCIAS ABIERTAS 3 · 1 sin gestionar`. Luego, dos
columnas: el mapa como escenario —con un distintivo de nivel arriba a la izquierda
(`NIVEL 1 · COMUNA`, `52 comunas`, `Agrupaciones ›`), una leyenda al pie (rampa de carga,
incidencia abierta, última posición del conductor) y la **banda de olas dentro de la columna
del mapa, bajo él**— y a la derecha un panel de **dos** pestañas (`Conductores · 7`,
`Comunas · 52`) rematado por una **ficha del seleccionado**. Al pie del panel, una línea que
explica la discrepancia Torre ↔ Pedidos. En 390 la disposición **se invierte**: dos cifras,
lista, y el mapa como bloque desplegable (`Ver el mapa ▾`).

### Qué tiene el código hoy

- Orden vertical: cabecera → `BandaOlas` → `CifrasTorre` → región mapa+panel
  (`torre.tsx:233-485`). La banda va **arriba** de las cifras, con el porqué escrito
  (`torre.tsx:11-13`).
- Cabecera: `h1` + una línea «{courier} · faltan N de M por entregar» e `IndicadorEnVivo`
  (`torre.tsx:237-255`). No hay distintivo de solo lectura ni de pantalla completa en la
  cabecera; el botón de pantalla completa vive **dentro** del mapa, arriba a la derecha
  (`torre.tsx:388-400`).
- Cuatro magnitudes en `cifras.tsx:60-89`: `Faltan por entregar N de M` · `Entregados hoy` ·
  `Incidencias abiertas` · `Cerca del corte` / `Pasadas del corte`. Más dos líneas
  condicionales: `sinUbicar` (`cifras.tsx:94-100`) y frescura atrasada (`:105-109`).
- Mapa MapLibre con tres niveles y migas de pan «Región Metropolitana / Comuna / Puntos de
  entrega» (`torre.tsx:334-386`). Franja de degradado «El plano urbano no cargó»
  (`:413-423`), velo «No hay pedidos con compromiso para hoy» (`:426-434`) y atribución
  (`:438-445`).
- Panel de **tres** pestañas: `Incidencias` (con contador rojo), `Conductores`, `Comunas`
  (`panel.tsx:77-88`). Ancho fijo 340 px, de `lg:` en adelante (`torre.tsx:470-482`).
- Fila de conductor (`panel.tsx:133-176`): nombre + «N de M» (o «N sin entregar» tras el
  cierre) + barra de avance. Sin porcentaje escrito, sin iniciales, sin nombre de ruta, sin
  comuna.
- Fila de comuna (`panel.tsx:178-210`): nombre + punto rojo si hay incidencia + «faltan N de
  M»; el clic **entra en la comuna en el mapa**.
- `< lg` el mapa se retira entero y manda `ListaComunas` (`torre.tsx:261-263`, `:461-467`).
- Contrato: `EstadoTorre` en `src/modules/contexto/contrato-torre.ts` — `resumen` trae
  `total, pendientes, entregados, incidenciasAbiertas, enRiesgoDeCorte, sinUbicar`
  (`:357-373`). No existe «en ruta ahora», ni «conductores con ruta», ni «incidencias sin
  gestionar».
- `avanceDeConductores` se arma **solo con las paradas del manifiesto de hoy**
  (`src/modules/contexto/agregacion.ts:373-396`): un conductor sin paradas no aparece.
- `posicion` del conductor es **siempre `null`** (rastreo apagado por privacidad,
  `agregacion.ts:349-371`); el marcador existe pero hoy no dibuja nada
  (`torre.tsx:304-313`).

### Delta

1. **Dos de las cuatro magnitudes son otras.** Falta `EN RUTA AHORA` y
   `CONDUCTORES CON RUTA 7 de 9`; sobran «Entregados hoy» y «Cerca del corte». Ninguna de las
   dos del tablero existe en `ResumenTorre` — es cambio de contrato del composer, no de
   rótulo.
2. **La cifra de incidencias no lleva su segunda línea** («3 · 1 sin gestionar»). «Sin
   gestionar» no viaja en `IncidenciaEnTorre` (`contrato-torre.ts:229-241`).
3. **El panel tiene tres pestañas y el tablero dos.** El tablero saca `Incidencias` del panel
   —queda en la cifra— y deja `Conductores` y `Comunas`, ambas con su cuenta en el rótulo.
4. **Falta la ficha del seleccionado al pie del panel** (`FICHA · ÑUÑOA`: Por entregar /
   Entregados / Conductores en la comuna / Incidencias abiertas / `Ver los 14 en Pedidos ›`).
   `ficha.tsx` es hoy la tarjeta del **punto del mapa**, no la de la comuna. El dato existe en
   `ComunaEnTorre` salvo «conductores en la comuna».
5. **La fila de avance de conductor está incompleta:** faltan iniciales, nombre de ruta
   («R. Muñoz · ruta 12»), comuna en curso, incidencias del conductor y el porcentaje escrito
   a la derecha.
6. **No aparece el conductor no disponible.** El tablero lo pide apagado y sin porcentaje
   («J. Tapia · no disponible hoy»); el agregador solo conoce a quien tiene paradas.
7. **Falta el distintivo `SOLO LECTURA` con trama** en la cabecera: cero ocurrencias en el
   módulo.
8. **Falta el distintivo de nivel sobre el mapa** (`NIVEL 1 · COMUNA · 52 comunas ·
   Agrupaciones ›`). Hoy eso lo cubren las migas de pan, que dicen menos.
9. **Falta la leyenda del mapa** (rampa de carga 0→14 con rótulo, incidencia abierta, última
   posición del conductor). No existe en `mapa.tsx`.
10. **La banda de olas está en otro sitio.** Tablero: dentro de la columna del mapa, debajo.
    Código: sobre las cifras, con justificación escrita. Hay que decidir cuál gana.
11. **Falta la línea de discrepancia Torre ↔ Pedidos** al pie del panel. Hoy solo vive en
    comentarios de código. El tablero la marca `NUEVO · APRUEBA O DESCARTA`.
12. **En 390 no hay inversión, hay amputación.** El tablero pide dos cifras + lista + mapa
    desplegable; el código muestra las cuatro cifras y **elimina el mapa** bajo `lg`, sin
    `Ver el mapa ▾`.
13. **Falta el estado «Nadie tiene paradas asignadas hoy» con su acción `Ir a asignar`.** El
    texto existe como vacío de la pestaña de conductores (`panel.tsx:134-135`), sin acción y
    sin ser estado de pantalla.
14. **`src/modules/contexto/mensajes-estado.ts` no lo consume nadie** (grep: solo su propio
    import de tipo). Los cuatro estados del tablero están hoy repartidos a mano en
    `torre.tsx`.

### Contradicciones anotadas (no se resuelven acá)

- **Ninguna sobre datos personales.** El tablero muestra en el mapa el código de envío y
  agregados por comuna, y en el panel nombre de conductor y comuna: no muestra dirección ni
  nombre del destinatario. Coincide con la regla dura.
- **La Torre sigue siendo de solo lectura en el tablero** — el clic en una comuna lleva a
  Pedidos ya filtrado, fuera de la Torre. Sin conflicto con `ver_torre_control`.
- **`ÚLTIMA POSICIÓN DEL CONDUCTOR` aparece en la leyenda del tablero**, pero el rastreo está
  apagado por privacidad desde el 14-08 y `posicion` es `null` siempre. La leyenda se puede
  construir; el dato no llegará hasta que exista `punto_termino_conductor`.

### Veredicto

`FALTA PIEZA` — la organización del tablero ya está en el código (cifras arriba, mapa como
escenario, lista lateral con pestañas, ficha, degradado declarado). Lo que falta son piezas
enumerables, pero **dos tocan el contrato del composer** (magnitudes 2 y 3, «sin gestionar»)
y una toca la agregación (conductor no disponible): no es solo trabajo de pantalla.

---

## 2 · Preparación del día

**Ruta real:** `src/app/(tenant)/preparacion/` — **SÍ existe** (Etapa 5 del alcance «retiro en
bodega + ruteo»). `page.tsx`, `_componentes/franja-magnitudes.tsx`, `lista-visitas.tsx`,
`tarjeta-visita.tsx`, `reloj-visita.tsx`, `carga-por-comuna.tsx`, `bloque-asignacion.tsx`,
`_lib/estado-preparacion.ts`, `_lib/reloj-inactividad.ts`. Datos:
`src/modules/operacion/retiro/preparacion.ts`.

### Qué muestra el tablero

Mismo patrón de monitoreo en vivo **sin mapa**, 1024 claro. Encabezado con
`21-08 11:40 · en vivo · faltan 4 h 20 para el despacho` y distintivo **en vivo**. Franja de
cuatro magnitudes: `BULTOS RETIRADOS HOY 128 de ~190` · `VISITAS EN CURSO 2 de 5` ·
`COMUNAS CON CARGA 11` · `SIN TARIFA 3 bultos`. Debajo, dos columnas: a la izquierda
`VISITAS A BODEGA EN CURSO` con tarjetas encabezadas por **seller · bodega**
(`Vega Norte · Bodega Maipú`), conductor y avance con denominador
(`R. Muñoz · 42 de ~70 escaneados`), un **reloj de visita** (`38 min`, `1 h 12`) y un
distintivo `abierta`; al pie, una línea que colapsa lo cerrado
(`3 visitas cerradas hoy · 74 bultos`). A la derecha,
`CARGA POR COMUNA · LO QUE YA ESTÁ EN BODEGA` como **barras** con la cifra en mono a la
derecha. Cierran dos líneas: el aviso de **comuna sin tarifa** con su acción
(`Configurar la tarifa de Colina`) y el **cálculo de conductores necesarios**. En 390 las dos
columnas se apilan y las visitas van primero.

### Qué tiene el código hoy

- Encabezado `h1` + subtítulo calculado por `calcularSubtituloCabecera`
  (`_lib/estado-preparacion.ts:137-160`) e `IndicadorEnVivo` sobre `sesiones_retiro`,
  `bultos_retiro` y `pedidos` (`page.tsx:130-143`).
- Franja de cuatro magnitudes (`franja-magnitudes.tsx:44-57`): `Bultos retirados hoy` ·
  `En bodega ahora` · `De vuelta` · `Sin novedades`. Ninguna lleva denominador. Línea extra
  condicional de `bultosSinIdentificar` (`:59-65`).
- Dos columnas `lg:grid-cols-[minmax(0,1fr)_320px]` — visitas a la izquierda, carga por comuna
  a la derecha (`page.tsx:156-163`). En una columna bajo `lg`, visitas primero.
- `ListaVisitas` (`lista-visitas.tsx:36-64`) parte en dos grupos con encabezado propio:
  `En bodega ahora (N)` y `De vuelta (N)`, ambos con **tarjeta completa**.
- `TarjetaVisita` (`tarjeta-visita.tsx:99-147`): título = **nombre del conductor**, luego
  `bodega · comuna`, luego seller. Cifra = `N escaneados` (abierta) o `Acta: N bultos`
  (cerrada). Sin denominador, sin distintivo de estado.
- `RelojVisita` mide **inactividad desde el último escaneo**, con umbral
  `UMBRAL_RETIRO_SIN_NOVEDADES_MINUTOS = 10` (`_lib/reloj-inactividad.ts:33`, `:84`), y se
  tiñe de `warning` sobre ese umbral (`reloj-visita.tsx:82-86`).
- `CargaPorComuna` (`carga-por-comuna.tsx:119-158`): lista con borde, `comuna` + `N bultos` +
  `«N asignados · M por asignar»`. Cada fila enlaza a `/preparacion/asignar?comuna=X`. **Sin
  barras.**
- `BloqueAsignacion` (`bloque-asignacion.tsx:206-232`): caja con borde punteado, conteo de
  «pedidos retirados sin asignar» y botón `Asignar pedidos`. **No está en el tablero.**
- Vacío: `EmptyState` tono `arranque`, «Todavía no hay retiros hoy», sin acción
  (`page.tsx:145-151`).
- Falla de lectura: dos consultas independientes (`page.tsx:87-96`); la franja cae a guiones
  (`franja-magnitudes.tsx:46-54`) y cada bloque muestra su propio `role="alert"` con «Intenta
  recargar la página».
- RBAC: `puedeVerPreparacionDia` con bloque `ShieldAlert` en la propia página
  (`page.tsx:57-72`).

### Delta

1. **Ninguna magnitud lleva denominador.** El tablero pide `128 de ~190` y `2 de 5`; el
   contrato `VisitaRetiroResumenCourier` (`preparacion.ts:89-103`) **no tiene ningún campo de
   bultos esperados**, así que el `~190` y el `~70` de la tarjeta no existen ni en la base.
2. **Dos de las cuatro magnitudes son otras.** Faltan `COMUNAS CON CARGA` y
   `SIN TARIFA 3 bultos`; sobran `De vuelta` y `Sin novedades`.
3. **Falta el aviso de comuna sin tarifa con su acción** («Colina no tiene tarifa configurada,
   así que esas 3 entregas no se podrían cobrar · Configurar la tarifa de Colina»). Cero
   ocurrencias: es la pieza que conecta esta pantalla con el motor de dinero.
4. **Falta el cálculo de conductores necesarios** («con 128 bultos y 12 min por parada
   necesitas 5 conductores para cerrar antes de las 21:00»). El tablero lo marca
   `NUEVO · APRUEBA O DESCARTA`.
5. **Falta la cuenta regresiva al despacho** en el subtítulo («faltan 4 h 20 para el
   despacho»). Hoy el subtítulo cuenta bultos y conductores, no el reloj de las 16:00.
6. **La tarjeta de visita tiene la jerarquía invertida.** Tablero: seller · bodega arriba,
   conductor debajo. Código: conductor arriba, bodega y seller debajo.
7. **Falta el distintivo `abierta` / `cerrada`** en la tarjeta.
8. **El reloj mide otra cosa y con otro umbral.** Tablero: tiempo transcurrido desde que se
   abrió la visita, con reparo **sobre una hora**. Código: minutos sin escanear, con reparo a
   los **10 minutos**. Son dos señales distintas, no un ajuste de constante.
9. **Las visitas cerradas no se colapsan.** El tablero las reduce a una línea
   (`3 visitas cerradas hoy · 74 bultos`); el código dibuja la tarjeta completa de cada una.
10. **«Carga por comuna» no tiene barras.** El tablero especifica barra + riel + cifra en mono
    a la derecha, y dice explícitamente que el riel es lo único que cambia entre temas
    (`#E4EDEE` claro / `#1B272C` oscuro).
11. **Al vacío le falta su acción** `Ver los retiros programados`; hoy el `EmptyState` no
    lleva ninguna.
12. **Falla de lectura: la franja cae a guiones**, y el tablero pide que las cifras
    «se queden con su última hora conocida» y que solo la lista muestre el bloque de
    reintento. Tampoco hay botón de reintento — solo el texto «Intenta recargar la página».
13. **El bloque `Asignación` del código no está en el tablero.** No es brecha del código: es
    una decisión que el tablero no contempla y hay que resolver (¿se conserva, se mueve, se
    integra en la línea de conductores necesarios?).

### Contradicciones anotadas (no se resuelven acá)

- El tablero muestra en las tarjetas nombres de conductor, seller y bodega, y comuna: ningún
  dato del destinatario. Coincide con lo que ya impone `tarjeta-visita.tsx:80-82`.
- El tablero no muestra ninguna acción de escritura en esta pantalla (la única acción es
  «Configurar la tarifa», que sale de aquí). Coincide con «la Etapa 5 no escribe nada»
  (`page.tsx:40-42`).

### Veredicto

`FALTA PIEZA` — la pantalla existe y ya tiene el patrón exacto del tablero (franja de
magnitudes sin cards + dos columnas, visitas primero en móvil, estados de error
independientes). Las brechas son enumerables, pero dos son caras: **los denominadores no
existen en el modelo de datos** (hace falta un «bultos esperados» por visita) y **el aviso de
comuna sin tarifa** exige cruzar la carga del día contra la tabla de tarifas.

---

## 3 · Incidencias

**Ruta real:** `src/app/(tenant)/operaciones/incidencias/` — **no** `(tenant)/incidencias/`.
Cuelga de Operaciones y la cabecera lleva una miga «‹ Pedidos». Archivos: `page.tsx`,
`filtros-incidencias.tsx`, `panel-incidencia.tsx`, `actions.ts`.

### Qué muestra el tablero

«Es la bandeja de P6, no un listado nuevo», 1440 oscuro. Encabezado con subtítulo que se
explica solo (`3 abiertas · 1 lleva más de 4 h sin gestionar`) y distintivo **en vivo**.
Debajo, **cajones** con su cuenta (`Abiertas 3`, `En gestión 5`, `Resueltas 28`,
`12 cerradas`) y dos filtros de añadir (`+ Tipo`, `+ Conductor`). La tabla se **agrupa por el
tiempo sin gestionar**, no por tipo: cabecera de grupo `SIN GESTIONAR · MÁS DE 4 H · 1` con
su nota («Genera aviso al centro de avisos y al correo de los internos»), luego
`RECIÉN ABIERTAS · 2`. Columnas: `TIPO Y PEDIDO` (tipo arriba, código + comuna debajo) ·
`ESTADO` · `ABIERTA HACE` · `CONDUCTOR` · `SELLER` · `AFECTA` (par de banderas **COBRO** y
**LIQ**). A la derecha, un **panel de caso** con: lo que reportó el conductor (cita textual,
autor, hora y «2 fotos»), fichas de transición `PASAR A` con solo los destinos válidos, el
efecto en el dinero como dos banderas con su explicación, un `RECLASIFICAR` desplegable,
`NOTAS DE RESOLUCIÓN` y dos acciones de cierre (`Reagendar para mañana`,
`Devolver al seller`).

### Qué tiene el código hoy

- Cabecera: miga `‹ Pedidos` + `h1` + `IndicadorEnVivo` (`page.tsx:177-192`). **Sin
  subtítulo.**
- Barra de filtros clásica con cuatro selects/fechas — seller, tipo, estado, rango de fecha
  (`filtros-incidencias.tsx:85-174`). **No hay cajones**, aunque el componente existe en el
  repo (`src/components/ui/barra-cajones.tsx`, usado por `/operaciones`).
- Tabla plana, sin agrupación (`page.tsx:218-269`). Columnas: `Estado` · `Tipo` · `Pedido` ·
  `Seller` · `Abierta hace`, más una columna de acción con el botón `Gestionar` si
  `puedeGestionarIncidencias`.
- «Abierta hace» muestra `Nh` y, si pasa el umbral, se pone en `text-destructive` con el
  sufijo `(sin gestión)` (`page.tsx:252-257`). El umbral es
  `UMBRAL_INCIDENCIA_SIN_GESTION_HORAS = 4` (`src/lib/ui/traduccion-estados.ts:396`) — **el
  mismo del tablero**.
- Filtro por defecto: `estado in ('abierta','en_gestion')`, orden por `abierta_en` desc,
  `limit(100)` (`page.tsx:45-59`).
- `PanelIncidencia` (`panel-incidencia.tsx:57-168`): cajón lateral abierto desde el botón
  `Gestionar`. Muestra Tipo, Estado actual, Descripción; un `textarea` de notas; y botones de
  transición según estado (`abierta → en_gestion`, `en_gestion → resuelta` con nota
  obligatoria, `resuelta → cerrada`) más `Guardar nota`.
- El tipo `Incidencia` **ya trae `afectaCobro` y `afectaLiquidacion`**
  (`src/modules/operacion/tipos.ts:371-372`), pero esta pantalla no los usa.
- La explicación del efecto en el dinero existe, pero **en otra pantalla y como prosa**:
  `src/app/(tenant)/operaciones/[pedidoId]/drawer-incidencia.tsx:26-31`.
- Reclasificar existe, pero **en otra pantalla**:
  `src/app/(tenant)/operaciones/[pedidoId]/dialog-reclasificar-incidencia.tsx`.

### Delta

1. **No hay cajones.** El tablero organiza por estado con cuenta visible; el código lo hace
   con un `Select` de estado y un default implícito.
2. **No hay agrupación por tiempo sin gestionar.** Es *la* decisión del tablero («agrupa por
   tiempo, no por tipo… es la unidad de decisión del supervisor») y hoy no existe: la tabla es
   una sola lista ordenada por fecha. El dato para hacerlo ya está (`esIncidenciaSinGestion`).
3. **Faltan dos columnas: `CONDUCTOR` y `AFECTA`.** `AFECTA` es solo render (los campos ya
   están en el tipo); `CONDUCTOR` **no está en `Incidencia`** y exige resolverlo por el pedido,
   igual que ya se hace con la referencia del pedido (`page.tsx:83-109`).
4. **La columna de pedido no muestra la comuna.** El tablero pide `RX-3H8P-5MKL · Ñuñoa`.
5. **`TIPO Y PEDIDO` es una sola columna apilada** en el tablero; el código las tiene
   separadas y en orden inverso (Estado primero).
6. **Falta el subtítulo que explica la pantalla** («3 abiertas · 1 lleva más de 4 h sin
   gestionar»). El tablero insiste en que el supervisor aterriza aquí desde un aviso.
7. **Falta la nota de la cabecera de grupo** que dice que ese grupo dispara aviso al centro de
   avisos y al correo.
8. **El panel de caso es otro objeto.** Faltan: la cita de lo que reportó el conductor con
   autor, hora y fotos; las fichas de transición `PASAR A` con destinos válidos declarados y
   la nota de por qué «Cerrada» no aparece; las dos banderas COBRO/LIQ con su explicación; el
   desplegable de reclasificar; y las dos acciones de resolución
   (`Reagendar para mañana`, `Devolver al seller`) con «quedan a tu nombre».
9. **El panel se abre desde una columna de acción por fila**, no desde la selección de la
   fila; el tablero lo trata como panel de caso del elemento seleccionado.
10. **Vacío genérico.** Hoy: «No hay incidencias para los filtros seleccionados». El tablero
    pide el estado de buena noticia: «Sin incidencias abiertas · Las 34 entregas de hoy
    cerraron sin problemas reportados · Última revisión: hoy 16:04».

### Contradicciones anotadas (no se resuelven acá)

- El tablero muestra **comuna** del pedido en la fila, y nombres de conductor y seller. No
  muestra dirección ni nombre del destinatario. Sin conflicto con la regla dura.
- Las banderas se llaman **COBRO** y **LIQ**, y el tablero avisa expresamente que **no** son
  `FACT`/`PAGO` (esas son las de la excepción de conciliación). Al construirlas hay que
  respetar el vocabulario propio de cada objeto.
- El tablero pone el reclasificar y el efecto en el dinero **dentro de la bandeja**; hoy viven
  en el detalle del pedido. Es duplicación o mudanza — hay que decidirlo, no parchearlo.

### Veredicto

`PANTALLA DISTINTA` — el tablero no pide columnas nuevas sobre la tabla actual: pide **otra
organización de la información** (cajones + agrupación por tiempo sin gestionar + panel de
caso completo con historial, banderas y resolución). El listado de hoy es un filtrado plano
con un cajón de acciones; convertirlo en la bandeja de P6 es rehacerlo. La única pieza que ya
calza es el umbral de 4 h.

---

## 4 · Manifiestos (listado)

**Ruta real:** `src/app/(tenant)/manifiestos/` — `page.tsx`, `filtros-manifiestos.tsx`,
`actions.ts`.

### Qué muestra el tablero

«Listado de P1 sin cambios», 1024 claro. Encabezado con subtítulo
`21-08 · 7 de 9 conductores con ruta`, un botón de filtros colapsado con su cuenta
(`Filtros 2`) y **cajones** con cuenta: `En ruta 5`, `Listos para salir 2`, `Borrador 1`,
`Completados 12`, `2 cancelados`, y el total `20 de 22`. Columnas:
`ESTADO` · `CONDUCTOR` (nombre + nombre de ruta) · `PARADAS` · `AVANCE` (la fila de avance de
conductor de B1a, reutilizada) · `SALIDA` (hora) · chevron. Dos filas son especiales: la de
**Borrador** usa la celda de avance para decir qué ve el conductor («Sin pedidos y sin
confirmar. El conductor ve “tu ruta todavía no está lista”») y la de **Cancelado** para el
resultado de la redistribución («Redistribuido · 18 paradas a 3 conductores»). El avance va en
tono de falla bajo el 40% **después de las 18:00**, y neutro antes.

### Qué tiene el código hoy

- Cabecera: `h1` + `IndicadorEnVivo` + botón `Asignar pedidos` (`page.tsx:116-134`). **Sin
  subtítulo.**
- Filtros siempre visibles: `Estado` (select con los 5 estados) y `Fecha de operación`
  (`filtros-manifiestos.tsx:70-99`). **No hay filtro por conductor** y no hay botón colapsado
  con cuenta.
- **No hay cajones.** El componente existe (`src/components/ui/barra-cajones.tsx`) pero esta
  pantalla no lo usa; la barra de herramientas solo dice «N manifiestos» (`page.tsx:177-181`).
- Tabla (`page.tsx:183-212`), cuatro columnas: `Estado` (`BadgeEstado`) · `Nombre` (enlace al
  detalle) · `Fecha` · `Conductor`. **No hay `Paradas`, `Avance`, `Salida` ni chevron.**
- Consulta: `manifiestos` ordenados por `fecha_operacion` desc, `limit(50)`, con filtros de
  estado y fecha (`page.tsx:69-83`). Solo la tabla de manifiestos: **no se cuenta ni una
  parada**.
- El tipo `Manifiesto` (`src/modules/operacion/tipos.ts`) trae `confirmadoEn` y
  `completadoEn` — el dato de «salida» existe, sin usar en el listado.
- Los cinco estados del código (`borrador`, `confirmado`, `en_ruta`, `completado`,
  `cancelado`, `tipos.ts:178-184`) **calzan uno a uno** con los cinco cajones del tablero.
- Vacío: `EmptyState` con dos tonos (`arranque` / `filtro`), texto propio y acción
  `Asignar pedidos` (`page.tsx:152-173`). Es más completo que lo del tablero salvo el título.

### Delta

1. **Faltan los cajones con cuenta** (`En ruta 5`, `Listos para salir 2`, `Borrador 1`,
   `Completados 12`, `2 cancelados`, `20 de 22`). Los estados ya calzan; falta el conteo por
   estado en la consulta y el componente, que ya existe.
2. **Faltan tres columnas: `PARADAS`, `AVANCE` y `SALIDA`.** Las dos primeras exigen contar
   `asignaciones_pedido` por manifiesto (hoy no se consulta); `SALIDA` sale de `confirmadoEn`,
   que ya está cargado.
3. **La columna de identidad es otra.** Tablero: `CONDUCTOR` con nombre y nombre de ruta.
   Código: `Nombre` del manifiesto como enlace, y `Conductor` como columna secundaria oculta
   bajo `md`.
4. **Falta la columna `Fecha` en el tablero** y sobra en el código — el tablero la resuelve en
   el subtítulo (`21-08 · …`) porque el listado es del día.
5. **Falta el subtítulo** `N de M conductores con ruta`.
6. **Falta el chevron de fila** (afordancia de «entra al detalle»); hoy solo el nombre enlaza.
7. **Falta el umbral de avance con hora** (tono de falla bajo 40% **solo después de las
   18:00**). No existe ninguna regla de este tipo en la pantalla.
8. **Falta la celda que dice qué ve el conductor en `Borrador`.** El tablero la marca
   `NUEVO · APRUEBA O DESCARTA` y es la brecha del callejón sin salida de la app.
9. **Falta el resultado de la redistribución en la fila `Cancelado`**
   («Redistribuido · 18 paradas a 3 conductores»).
10. **Falta el filtro por conductor** y el patrón de filtros colapsados con cuenta.
11. **El título del vacío no calza**: «Aún no tienes manifiestos» vs. «Aún no hay manifiestos
    hoy»; el tablero además explica el mecanismo («Se crean solos al asignar pedidos a un
    conductor»), que el código sí dice en la descripción.

### Contradicciones anotadas (no se resuelven acá)

- Ninguna. El tablero no muestra ningún dato personal del destinatario en este listado, y las
  acciones que propone (entrar al detalle) ya son las del código.

### Veredicto

`FALTA PIEZA` — la organización es la misma (cabecera + filtros + tabla + vacío con acción) y
el delta es una lista cerrada de columnas, cajones y textos. Lo caro no es la pantalla: es que
**`PARADAS` y `AVANCE` obligan a agregar paradas por manifiesto en la consulta**, que hoy no
toca `asignaciones_pedido` en absoluto.

---

## 5 · Detalle del manifiesto — el secuenciador de ruta

**Ruta real:** `src/app/(tenant)/manifiestos/[manifiestoId]/` — `page.tsx`, `panel-ruta.tsx`,
`actions-ruta.ts`, `boton-quitar-pedido.tsx`, `boton-confirmar-manifiesto.tsx`,
`boton-cancelar-manifiesto.tsx`, `boton-completar-manifiesto.tsx`. Motor y escritura:
`src/modules/operacion/ruta-manifiesto.ts`, `ruteo/`, `secuencia-paradas-rpc.ts`,
`distancias-tramo.ts`, `orden-paradas.ts`.

> **Verificado antes de declarar nada:** el secuenciador **existe y funciona**. No es un
> hueco. `PanelRuta` recalcula los tramos en el navegador con la misma función pura del
> servidor, ofrece los tres controles del tablero (↑, ↓ y campo «a la N.º»), manda siempre la
> secuencia completa —nunca un delta— y distingue el error de desincronización del error de
> red. `accionCalcularRuta` y `accionGuardarOrdenManual` llaman al motor y al RPC.

### Qué muestra el tablero

Página completa, 1440 oscuro, con estructura de P3. Encabezado:
`‹ Manifiestos · hoy · en ruta`, título `R. Muñoz · ruta 12`, subtítulo
`21-08 · Bodega Quilicura → punto de término en Ñuñoa`, tres distintivos (`En ruta`,
`28 paradas · 14 cerradas`, `185,3 km ruteada`) y dos acciones a la derecha
(`Imprimir la hoja de ruta`, `···`). Debajo, **dos columnas**. La ancha:
`SECUENCIA DE LA RUTA` con `Recalcular la ruta` y `Guardar el orden manual`, y una tabla de
`N.º` · `DIRECCIÓN` (dirección arriba, comuna debajo — con reparos como `Colina · sin tarifa`)
· `DESTINATARIO` · `ESTADO` · `MOVER` (`↑ ↓ a la N.º`). Las cerradas van **rayadas y no se
mueven**; la parada en curso se rotula `Siguiente`. Bajo la tabla, el **panel de recálculo
antes de guardar**: `Orden actual 185,3 km` / `Con tu cambio 198,7 km · +13,4` y la frase de
holgura («Sigues cerrando antes de las 21:00, con 40 min de margen»), con
`Guardar este orden` y `Volver al orden calculado`. La columna angosta: `ACCIONES` (quitar una
parada — peldaño 2 con su consecuencia escrita; agregar pedidos desde la bandeja — peldaño 1),
`Zona de consecuencia · TODO EN LA BITÁCORA` (marcar al conductor no disponible y
**Redistribuir**; **Cancelar** el manifiesto — las dos piden motivo, y redistribuir devuelve
tarjeta de resultado en bloque) y `BITÁCORA` con las últimas entradas con hora y autor.

### Qué tiene el código hoy

- **Una sola columna** (`page.tsx:246-374`): retorno, encabezado, panel de ruta, fila de
  acciones, avisos por estado.
- Encabezado: `h1` = **nombre del manifiesto** (no el conductor), línea
  `Conductor: X — Fecha: Y`, notas, y un `Badge` de estado a la derecha (`page.tsx:252-271`).
  Sin distintivos de paradas/cerradas/km, sin acción de imprimir, sin menú `···`.
- `PanelRuta` (`panel-ruta.tsx:129`, cabecera en `:266-292`): título `Ruta del conductor (N)`,
  subtítulo `Sale desde {bodega} · {total} en línea recta`, y los botones — `Calcular ruta`
  cuando no hay cambios, `Descartar` + `Guardar orden` cuando sí (`:294-319`).
- Tabla de paradas (`panel-ruta.tsx:371-509`), columnas: `#` · **`Tramo`** (distancia desde la
  parada anterior, visible también en móvil por decisión explícita) · `Estado` ·
  `Destinatario` (enlace al pedido, con la comuna como sufijo y distintivo `Sin ubicación`) ·
  `Dirección` (oculta `< sm`) · `F. compromiso` (oculta `< md`) · acciones.
- Controles de movimiento (`panel-ruta.tsx:437-492`): ↑, ↓ y **campo numérico de posición**
  con `Enter` y `blur` — los tres del tablero, ya construidos.
- `BotonQuitarPedido` (`:494-500`) en la misma fila, solo si el manifiesto está en borrador.
- Avisos: paradas sin coordenada (`:344-355`), cambios sin guardar (`:357-362`), «todavía no
  tiene ruta calculada… el conductor lo ve ordenado alfabéticamente» (`:511-517`).
- Acciones por estado (`page.tsx:312-373`): `Agregar pedidos`, `Confirmar`, `Cancelar` (solo
  en borrador); aviso «Ver como lo ve el conductor» en confirmado; `Completar` en ruta, con
  advertencia de paradas abiertas.
- Vacío único: «Este manifiesto no tiene pedidos todavía» (`page.tsx:298-309`).
- **`actionMarcarConductorNoDisponible` con su `ResultadoRedistribucion` existe**
  (`manifiestos/actions.ts:131-172`), pero **el llamador está en
  `(tenant)/conductores/panel-conductores.tsx`**, no en esta pantalla.
- **La ruta persistida se comparte con las tres pantallas** vía
  `ordenarParadasConSecuencia` (`page.tsx:203-206`), y el alfabético queda como respaldo
  legítimo, tal como dice el alcance.

### Delta

1. **No hay segunda columna.** Faltan las tres regiones que el tablero pone a la derecha:
   `ACCIONES`, `Zona de consecuencia` y `BITÁCORA`. Hoy las acciones son una fila suelta bajo
   la tabla y no hay bitácora visible en pantalla.
2. **Falta el panel de recálculo antes de guardar** (`Orden actual` vs. `Con tu cambio`, el
   delta en km y minutos, y la frase de holgura contra las 21:00). Hoy el coordinador ve el
   tramo por fila y el total en el subtítulo, pero **no ve el antes/después de su cambio** ni
   si rompe el turno. La frase de holgura está marcada `NUEVO · APRUEBA O DESCARTA`.
3. **Las paradas cerradas no se distinguen ni se bloquean.** El tablero las raya y les quita
   los controles («Reordenar solo afecta a las pendientes»); el código deja ↑/↓/posición
   activos en todas.
4. **Falta el rótulo `Siguiente` / `en curso`** en la parada activa.
5. **El orden de columnas está invertido respecto de la corrección del tablero.** Tablero:
   `DIRECCIÓN` primero, `DESTINATARIO` después — «igual que en la app, es la corrección de la
   pasada 2 aplicada». Código: `Destinatario` primero y `Dirección` **oculta bajo `sm`**.
6. **Faltan los reparos en la celda de dirección**, en concreto `Colina · sin tarifa`.
7. **El encabezado es otro.** Falta el título por conductor y ruta, el subtítulo
   `Bodega → punto de término`, y los tres distintivos (`28 paradas · 14 cerradas`,
   `185,3 km ruteada`). Ojo: **el punto de término no puede aparecer** (ver contradicciones).
8. **Falta `Imprimir la hoja de ruta`.** Cero ocurrencias en el repo: no hay ruta de impresión
   ni endpoint.
9. **Falta `Redistribuir` en esta pantalla.** La acción y su resultado en bloque ya existen;
   lo que falta es traerla aquí y **pedir motivo**.
10. **`Cancelar el manifiesto` no pide motivo** (`boton-cancelar-manifiesto.tsx`, sin campo de
    texto). El tablero dice que las dos lo piden.
11. **Faltan los peldaños de las acciones destructivas.** El tablero marca quitar una parada
    como **peldaño 2 con consecuencia escrita** («devuelve el pedido a la bandeja sin
    conductor… hoy se ejecuta al primer clic, a las 15:50»).
12. **Falta el estado de falla de lectura de paradas.** Hoy cualquier error cae en el vacío
    «no tiene pedidos» (`page.tsx:297-310`) — exactamente lo que el tablero señala como
    peligroso: «El manifiesto existe y tiene 28 paradas. No lo canceles ni redistribuyas hasta
    poder verlas».
13. **El vacío no dice qué ve el conductor.** Tablero: «J. Tapia ve “tu ruta todavía no está
    lista”. Agrégale pedidos o cancélalo para que pueda ir a retirar».
14. **Los controles no están dimensionados para el dedo** (48 px en el tablero); hoy son
    botones `p-1.5` y un campo `h-8 w-14`.

### Contradicciones anotadas (no se resuelven acá)

- 🚨 **El subtítulo del tablero dice `Bodega Quilicura → punto de término en Ñuñoa`.** El
  código lo prohíbe a propósito: `panel-ruta.tsx:41-51` y
  `src/modules/operacion/ruta-manifiesto.ts:13-42` documentan que el punto de término del
  conductor **no llega a la interfaz y la salida debe ser idéntica exista o no**, porque bajo
  subordinación laboral el consentimiento solo es libre si negarse no queda a la vista del
  jefe (`docs/seguridad/punto-de-termino-conductor.md` §4). **Mostrarlo reabre la revisión de
  `seguridad-cumplimiento`.** Es la contradicción más seria de este documento.
- **La columna `Tramo` no está en el tablero**, y el código la defiende como la razón de ser
  del reordenamiento manual («un “12,4 km” entre dos paradas seguidas sí se ve»). El tablero
  la sustituye por el panel de antes/después. Hay que decidir si conviven o si una reemplaza a
  la otra.
- **El tablero muestra dirección y nombre del destinatario**, y aquí eso **es correcto**: es
  la hoja de ruta operativa del courier, no la Torre. La regla de minimización aplica a la
  Torre, no a esta pantalla.

### Veredicto

`FALTA PIEZA` — y es el veredicto menos malo del lote justamente porque **lo caro ya está
construido**: el motor, la escritura de la secuencia, el recálculo en vivo de tramos y los
tres controles de movimiento. Lo que falta es la mitad derecha de la página (acciones con
peldaños, zona de consecuencia con motivo, bitácora), el panel de antes/después, el bloqueo de
las paradas cerradas y dos estados de pantalla. Nada de eso obliga a rehacer el secuenciador.

---

## Lo más caro de estas cinco

1. **Incidencias es la única `PANTALLA DISTINTA`.** No son columnas nuevas: son cajones,
   agrupación por tiempo sin gestionar y un panel de caso con el reporte del conductor, sus
   fotos, las banderas COBRO/LIQ y las dos acciones de resolución. Hoy es un filtrado plano
   con un cajón de acciones. Se rehace.
2. **Cuatro cifras del tablero no existen en ninguna capa de datos**, así que no son trabajo
   de pantalla: `EN RUTA AHORA` y `CONDUCTORES CON RUTA` (contrato de la Torre),
   `PARADAS`/`AVANCE` del listado de manifiestos (`asignaciones_pedido` no se consulta) y los
   denominadores de Preparación (`~190`, `~70`, no hay «bultos esperados» en el modelo).
3. **El punto de término del conductor que el tablero pone en el detalle del manifiesto está
   prohibido en el código por una revisión de privacidad.** No es una brecha de
   implementación: es una decisión que hay que reabrir con `seguridad-cumplimiento` antes de
   dibujar esa línea.

## Piezas sueltas encontradas de paso

- `src/modules/contexto/mensajes-estado.ts` define los mensajes de estado de la Torre y
  **nadie lo importa**.
- `src/components/ui/barra-cajones.tsx` existe y solo lo usa `/operaciones`: Incidencias y
  Manifiestos podrían reusarlo.
- `actionMarcarConductorNoDisponible` (`manifiestos/actions.ts:131-172`) vive en el módulo de
  manifiestos pero se llama desde `(tenant)/conductores/panel-conductores.tsx`.
- La ruta de Incidencias es `(tenant)/operaciones/incidencias/`, no `(tenant)/incidencias/`.
